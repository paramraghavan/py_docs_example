from setuptools import setup, find_packages

setup(
    name='py_docs_example',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # List your project dependencies here
    ],
)
